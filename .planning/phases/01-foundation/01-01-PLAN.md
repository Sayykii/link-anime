---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/database/database.go
autonomous: true
requirements:
  - DB-01

must_haves:
  truths:
    - "upscale_jobs table exists in SQLite database"
    - "Table has status column accepting pending/running/completed/failed/cancelled"
    - "Table has timestamp columns for job lifecycle tracking"
  artifacts:
    - path: "internal/database/database.go"
      provides: "upscale_jobs migration"
      contains: "CREATE TABLE IF NOT EXISTS upscale_jobs"
  key_links:
    - from: "internal/database/database.go"
      to: "SQLite database"
      via: "migrate() function execution"
      pattern: "upscale_jobs"
---

<objective>
Add upscale_jobs table migration to SQLite database

Purpose: Establish persistent storage for upscale job records with status tracking
Output: Migration added to database.go that creates upscale_jobs table on startup
</objective>

<execution_context>
@/home/desktop/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/desktop/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md

@internal/database/database.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add upscale_jobs migration</name>
  <files>internal/database/database.go</files>
  <action>
Add a new migration string to the `migrations` slice in the `migrate()` function.

The migration SQL should create the `upscale_jobs` table with:
- `id` INTEGER PRIMARY KEY AUTOINCREMENT
- `input_path` TEXT NOT NULL (source video file path)
- `output_path` TEXT NOT NULL (destination 4K file path)
- `preset` TEXT NOT NULL DEFAULT 'balanced' (quality preset)
- `status` TEXT NOT NULL DEFAULT 'pending' (job state: pending/running/completed/failed/cancelled)
- `error` TEXT (nullable - stores failure reason)
- `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP
- `started_at` DATETIME (nullable - when job began processing)
- `completed_at` DATETIME (nullable - when job finished)

Follow existing migration pattern: use `CREATE TABLE IF NOT EXISTS` to ensure idempotency.

Place the new migration after existing migrations in the slice.
  </action>
  <verify>
Run `go build ./...` to verify syntax is correct.
Optionally run the app briefly to confirm migration executes without error.
  </verify>
  <done>
Migration string added to database.go migrations slice.
Build succeeds with no errors.
Table will be created on next app startup.
  </done>
</task>

</tasks>

<verification>
1. `go build ./...` completes without errors
2. Migration SQL follows existing patterns (IF NOT EXISTS, same indentation style)
3. All required columns present with correct types and defaults
</verification>

<success_criteria>
- upscale_jobs migration added to database.go
- Schema matches research specification exactly
- Code compiles successfully
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
