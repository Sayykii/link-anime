---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/models/models.go
  - frontend/src/lib/types.ts
autonomous: true
requirements:
  - DB-02
  - DB-03
  - DB-04

must_haves:
  truths:
    - "UpscaleJob Go model serializes to JSON with camelCase keys"
    - "UpscaleProgress model captures real-time encoding metrics"
    - "TypeScript interfaces match Go model field names exactly"
    - "Status union type constrains valid status values"
  artifacts:
    - path: "internal/models/models.go"
      provides: "UpscaleJob and UpscaleProgress structs"
      contains: "type UpscaleJob struct"
    - path: "frontend/src/lib/types.ts"
      provides: "TypeScript UpscaleJob and UpscaleProgress interfaces"
      contains: "export interface UpscaleJob"
  key_links:
    - from: "internal/models/models.go"
      to: "frontend/src/lib/types.ts"
      via: "JSON field name mapping"
      pattern: "json:\"(\\w+)\""
---

<objective>
Create Go models and TypeScript interfaces for upscale jobs

Purpose: Enable type-safe data exchange between Go backend and Vue frontend
Output: UpscaleJob + UpscaleProgress structs in Go, mirrored interfaces in TypeScript
</objective>

<execution_context>
@/home/desktop/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/desktop/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md

@internal/models/models.go
@frontend/src/lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Go models to models.go</name>
  <files>internal/models/models.go</files>
  <action>
Add the following to `internal/models/models.go`:

1. Status constants (string type to match DB values exactly):
```go
const (
    UpscaleStatusPending   = "pending"
    UpscaleStatusRunning   = "running"
    UpscaleStatusCompleted = "completed"
    UpscaleStatusFailed    = "failed"
    UpscaleStatusCancelled = "cancelled"
)
```

2. UpscaleJob struct:
```go
type UpscaleJob struct {
    ID          int64      `json:"id"`
    InputPath   string     `json:"inputPath"`
    OutputPath  string     `json:"outputPath"`
    Preset      string     `json:"preset"`
    Status      string     `json:"status"`
    Error       *string    `json:"error,omitempty"`
    CreatedAt   time.Time  `json:"createdAt"`
    StartedAt   *time.Time `json:"startedAt,omitempty"`
    CompletedAt *time.Time `json:"completedAt,omitempty"`
}
```

3. UpscaleProgress struct (for WebSocket broadcasts):
```go
type UpscaleProgress struct {
    JobID   int64   `json:"jobId"`
    Frame   int     `json:"frame"`
    FPS     float64 `json:"fps"`
    Time    string  `json:"time"`    // e.g., "00:05:23"
    Percent float64 `json:"percent"` // 0-100
}
```

Follow existing patterns in the file:
- Use camelCase for JSON tags
- Use pointer types with `omitempty` for nullable fields
- Ensure `time` import is present
  </action>
  <verify>
Run `go build ./...` to verify syntax and imports.
  </verify>
  <done>
UpscaleJob and UpscaleProgress structs added to models.go.
Status constants defined.
Build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add TypeScript interfaces to types.ts</name>
  <files>frontend/src/lib/types.ts</files>
  <action>
Add the following to `frontend/src/lib/types.ts`:

1. UpscaleStatus union type:
```typescript
export type UpscaleStatus = 'pending' | 'running' | 'completed' | 'failed' | 'cancelled'
```

2. UpscaleJob interface:
```typescript
export interface UpscaleJob {
  id: number
  inputPath: string
  outputPath: string
  preset: string
  status: UpscaleStatus
  error?: string
  createdAt: string
  startedAt?: string
  completedAt?: string
}
```

3. UpscaleProgress interface:
```typescript
export interface UpscaleProgress {
  jobId: number
  frame: number
  fps: number
  time: string
  percent: number
}
```

Field names must match Go JSON tags exactly (camelCase).
Use optional `?` for fields that use `omitempty` in Go.
Timestamps are `string` (Go time.Time serializes to RFC3339 string).
  </action>
  <verify>
Run `cd frontend && npm run type-check` (or `tsc --noEmit`) to verify types compile.
  </verify>
  <done>
TypeScript interfaces added to types.ts.
Types mirror Go models exactly.
TypeScript compiles without errors.
  </done>
</task>

</tasks>

<verification>
1. `go build ./...` succeeds
2. `cd frontend && npm run type-check` succeeds (or equivalent TypeScript check)
3. JSON tags in Go match interface field names in TypeScript
4. Optional fields use pointer/*omitempty in Go and `?` in TypeScript
</verification>

<success_criteria>
- Go models compile and follow existing patterns
- TypeScript interfaces compile and mirror Go models
- Field mappings are consistent (camelCase, matching names)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
